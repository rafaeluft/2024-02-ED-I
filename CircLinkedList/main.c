#include <stdio.h>
#include "TCircList.h"

int main(){
    /* Descomente o seguinte código quando implementar as funções da TCircList.h */
    /* 
    TCircList* list = TCircList_create();
    int V[] = {1,2,3,4,5,6};
    for(int i = 0; i <6; i++)
        if(!TCircList_insert(list,V[i]))
            printf("Nao pude inserir o valor V[%d]=%d\n",i,V[i]);
    TCircList_print(list);
    */
    return 0;
}